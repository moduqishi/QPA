services:
  qpa:
    build: .
    container_name: qpa
    restart: unless-stopped
    ports:
      - "8963:8963"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./baseprompt.json:/app/baseprompt.json
    environment:
      - TZ=Asia/Shanghai
